# lambda
# a stricter version of the Proc
# - it will throw an Error if you give it the wrong number of arguments
# - the "return" keyword will only return from the lambda,
#   Thus, any return values will be made available to the enclosing method.